prefix=/usr
exec_prefix=${prefix}
libdir=${exec_prefix}/usr/lib/#DEB_HOST_MULTIARCH#
includedir=${prefix}/include

Name: TensorFlow
Version: #VERSION#
Description: Library for computation using data flow graphs for scalable machine learning
Requires:
Libs: -L${libdir} #LIBS#
Cflags: -I${includedir}
